/* 
- Linguagem interpretada. O código é executado de cima para baixo, e o resultado dessa execução é imediatamente retornado, não tem que transformar o código em algo diferente (bytecode) antes do navegador executar.

- Linguagem de tipagem fraca e dinâmica. Significa que não há verificação em todas as operações no JS, ou seja, é possivel o usar o operador + com uma string ou um inteiro, sem que ocorra um erro.

- TypeScript (superset da linguagem JS)
    
    - Adiciona tipos em tempo de desenvolvimento
    - Adiciona funcionalidades que o JS não tem por padrão (interface, enums, permite tipo generics,...).

- Funções de primeira classe e ordem maior. Função de primeira classe, esse termo significa que a função pode ser atribuída a uma variável, a uma estrutura de dados (object, array), podem ser passadas como argumentos, e até retornada por outras funções.

- Escopos

    // escopo global

    -------------------------

    {
        // escopo de bloco
    }

    if(...){
        
    }

    -------------------------
    
    function fn() {
        // escopo de função
    }
*/

